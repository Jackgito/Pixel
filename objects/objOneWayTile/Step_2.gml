//with (objDynamicParent) {
//    // Reapply the fixture when the object is not colliding with the platform and is falling
//    if (phy_speed_y >= 0 && !place_meeting(x, y, objOneWayTile)) {
//        physics_add_fixture(self, fixture);
//		physics_
//    }
//}