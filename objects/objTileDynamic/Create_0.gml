currentPower = 1; // Change glow intensity when power changes
image_speed = 0;
glowColor = c_white;
//choose(NEON_BLUE_LIGHT, NEON_GREEN_LIGHT, NEON_PINK_LIGHT, NEON_YELLOW_LIGHT, NEON_PURPLE_LIGHT);

// 1. Create a physics fixture
//var fixture = physics_fixture_create();

//// 2. Set the fixture as a box shape with the size of the sprite
//var width = sprite_width;
//var height = sprite_height;

//// Define the box shape, centered on the object's origin (0,0) relative to the sprite
//physics_fixture_set_box_shape(fixture, width / 2, height / 2);